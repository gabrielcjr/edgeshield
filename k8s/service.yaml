apiVersion: v1
kind: Service
metadata:
  name: edgeshield
  namespace: edgeshield
  labels:
    app: edgeshield
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30088
      protocol: TCP
      name: http
  selector:
    app: edgeshield
