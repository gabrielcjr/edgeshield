apiVersion: v1
kind: Service
metadata:
  name: edgeshield
  namespace: default
  labels:
    app: edgeshield
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: edgeshield
